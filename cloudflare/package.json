{
  "name": "scenario-share",
  "version": "0.1.0",
  "private": true,
  "description": "Scenario — public share page + video storage (R2) + view analytics (D1) on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.107.0"
  }
}
